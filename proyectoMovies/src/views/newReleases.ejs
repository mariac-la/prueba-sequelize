<!DOCTYPE html>
<html lang="en">

  <%- include('./partials/head')  %>

  <body>

    <%- include('./partials/header')  %>

    <section class = "contenedor-new">
      <h2 class="titulo-new">Estrenos</h2>
      <% newReleases.forEach(movie => { %>
        <a href="detail/<%=movie.id%>" class="nombre-new">
            <p>
                <%= movie.title %>
            </p>
        </a>
      <% }) %>
    </section>
  </body>
</html>
